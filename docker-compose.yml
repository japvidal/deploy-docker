name: tikitakas

services:
  mysql:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - /home/japerez/docker_volumes/mariadb/futfem.sql:/docker-entrypoint-initdb.d/futfem.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u root -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    networks:
      - tikitakas

  eureka:
    image: ${EUREKA_IMAGE}
    restart: unless-stopped
    environment:
      SERVER_PORT: 8761
      EUREKA_CLIENT_REGISTER_WITH_EUREKA: "false"
      EUREKA_CLIENT_FETCH_REGISTRY: "false"
    ports:
      - "8761:8761"
    networks:
      - tikitakas

  competitions:
    image: ${COMPETITIONS_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "8081:8080"
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  competitions-temp:
    image: ${COMPETITIONS_TEMP_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  players:
    image: ${PLAYERS_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  players-temp:
    image: ${PLAYERS_TEMP_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  teams:
    image: ${TEAMS_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  teams-temp:
    image: ${TEAMS_TEMP_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  positions:
    image: ${POSITIONS_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  referees:
    image: ${REFEREES_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  referees-temp:
    image: ${REFEREES_TEMP_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  squads:
    image: ${SQUADS_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  squads-temp:
    image: ${SQUADS_TEMP_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  matchdayplayerslocal:
    image: ${MATCHDAYPLAYERSLOCAL_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  matchdayplayersvisitante:
    image: ${MATCHDAYPLAYERSVISITANTE_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  matchdayteamlocal:
    image: ${MATCHDAYTEAMLOCAL_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  matchdayteamvisitante:
    image: ${MATCHDAYTEAMVISITANTE_IMAGE}
    restart: unless-stopped
    environment:
      PORT: 8080
      EUREKA_DEFAULT_ZONE: http://eureka:8761/eureka
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_started
    networks:
      - tikitakas

  zuul:
    image: ${ZUUL_IMAGE}
    restart: unless-stopped
    environment:
      SERVER_PORT: 8090
      EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE: http://eureka:8761/eureka
    ports:
      - "8090:8090"
    depends_on:
      eureka:
        condition: service_started
      competitions:
        condition: service_started
      competitions-temp:
        condition: service_started
      players:
        condition: service_started
      players-temp:
        condition: service_started
      teams:
        condition: service_started
      teams-temp:
        condition: service_started
      positions:
        condition: service_started
      referees:
        condition: service_started
      referees-temp:
        condition: service_started
      squads:
        condition: service_started
      squads-temp:
        condition: service_started
      matchdayplayerslocal:
        condition: service_started
      matchdayplayersvisitante:
        condition: service_started
      matchdayteamlocal:
        condition: service_started
      matchdayteamvisitante:
        condition: service_started
    networks:
      - tikitakas

  frontend:
    image: ${FRONTEND_IMAGE}
    restart: unless-stopped
    ports:
      - "4200:80"
    volumes:
      - ${FRONTEND_ASSETS_HOST_PATH}:/usr/share/nginx/html/assets/images:ro
    depends_on:
      zuul:
        condition: service_started
    networks:
      - tikitakas

volumes:
  mysql_data:

networks:
  tikitakas:
    driver: bridge
